<?xml version="1.0" encoding="utf-8"?>

<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent"
    android:padding="@dimen/padding_medium"
    tools:context=".main.admin.songs.AdminSongsMenuBottomSheetDialogFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="@drawable/bottom_sheet_round_bg"
        android:orientation="vertical"
        android:id="@+id/buttonsContainer"
        android:paddingTop="@dimen/padding_x_large"
        android:paddingBottom="@dimen/padding_x_large">

        <Button
            android:id="@+id/editSong"
            style="@style/Theme.HepiMusic.MenuButton"
            android:drawableEnd="@drawable/ic_add_playlist"
            android:text="@string/edit_song" />

        <Button
            android:id="@+id/delete"
            style="@style/Theme.HepiMusic.MenuButton"
            android:drawableEnd="@drawable/ic_garbage"
            android:text="@string/delete_frm_library" />

    </LinearLayout>

</FrameLayout>